# Images

Add your assets here.

Recommended:
- `og.png` (1200x630) — Open Graph / social preview
- Optional: simple brand textures or icons

This repo is configured with `images.unoptimized: true` to work with static export.
